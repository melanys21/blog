import { ContentTextPipe } from './content-text.pipe';

describe('ContentTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTextPipe();
    expect(pipe).toBeTruthy();
  });
});
